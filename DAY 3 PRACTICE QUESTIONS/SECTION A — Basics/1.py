# Q1
# Create NumPy array of:
# [5, 10, 15, 20, 25]

# Print:
# sum
# average
# max
# min
import numpy as np
arr = np.array([5, 10, 15, 20, 25])

print(np.sum(arr))
print(np.mean(arr))
print(np.max(arr))
print(np.min(arr))